{
  "_comment": "Drop this into ~/Library/Application Support/Claude/claude_desktop_config.json (macOS) or %APPDATA%/Claude/claude_desktop_config.json (Windows) — or merge the mcpServers block into your existing config. Restart Claude Desktop after editing.",
  "mcpServers": {
    "abs": {
      "command": "uvx",
      "args": ["--upgrade", "abs-mcp"]
    },
    "rba": {
      "command": "uvx",
      "args": ["--upgrade", "rba-mcp"]
    },
    "ato": {
      "command": "uvx",
      "args": ["--upgrade", "ato-mcp"]
    }
  }
}
